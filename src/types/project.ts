export interface ProjectData {
  title: string;
  tags: string[];
  description: string;
  repo: string;
}

export interface ExperienceData {
  title: string;
  duration: string;
  location: string;
  description: string;
}
